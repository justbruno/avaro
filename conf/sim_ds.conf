ITERATION_SLEEP=0.25 # Sleep time between main loop iterations
ISU_FACTOR = 0.005 # Speed of interpolation to current price bottom
DROP_THR = 0.01 # Price percentage drop at which we trigger a potential buy
